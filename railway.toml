[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma db push --schema=backend/prisma/schema.prisma --accept-data-loss --skip-generate && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
